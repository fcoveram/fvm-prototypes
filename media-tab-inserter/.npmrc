# The @wordpress/* packages declare React 19 peer deps while this prototype
# pins React 18, so installs must tolerate the mismatch. Setting it here keeps
# local installs, Vercel, and the CI `npm ci` all resolving the same way.
legacy-peer-deps=true
