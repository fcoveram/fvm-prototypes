{
	"name": "fvm-prototypes",
	"private": true,
	"version": "0.0.0",
	"description": "Standalone UI prototypes, each served at prototypes.fvm.house/<name>/",
	"scripts": {
		"build": "bash build.sh"
	},
	"engines": {
		"node": "22.x"
	}
}
